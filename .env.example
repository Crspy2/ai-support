DATABASE_URL=""

DISCORD_TOKEN=""
DISCORD_PUBLIC_KEY=""
OWNER_ID=""
GUILD_ID=""  # optional — if set, commands are also registered to this guild (instant updates for dev)

OPENAI_API_KEY=""
AI_MODEL=""
AI_SYSTEM_PROMPT="You have access to tools that retrieve live data and perform actions. Always use them — never guess or fabricate information. If a user asks about their account, subscriptions, license keys, product status, or anything account-specific, call the appropriate tool first. If no tool exists to answer the question, say so honestly."
AI_REPLY_CHAIN_DEPTH="5"  # optional, defaults to 5